{
    "schemaVersion": 1,
    "id": "${id}-1_21",
    "version": "${version}",
    "name": "${name} for 1.20.1",
    "description": "${description}",
    "authors": [
        "${author}"
    ],
    "contact": {
        "homepage": "${source}",
        "sources": "${source}",
        "issues": "${source}/issues"
    },
    "license": "MIT",
    "icon": "assets/${id}/icon.png",
    "environment": "*",
    "mixins": [
        "${id}.1.21.mixins.json"
    ],
    "depends": {
        "fabricloader": "${fabric_loader}",
        "minecraft": "${minecraft}",
        "fabric-language-kotlin": "${fabric_kotlin}"
    },
    "recommends": {
        "modmenu": "${modmenu}"
    },
    "custom": {
        "modmenu": {
            "parent": "$id"
        }
    }
}
